{% extends 'base.html.twig' %}

{% block title %}Prescri{% endblock %}

{% block body %}
    <h1>Prescri</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prescri.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ prescri.quantite }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ prescri.dateDebut ? prescri.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ prescri.dateFin ? prescri.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_prescri_index') }}">back to list</a>

    <a href="{{ path('app_prescri_edit', {'id': prescri.id}) }}">edit</a>

    {{ include('prescri/_delete_form.html.twig') }}
{% endblock %}
