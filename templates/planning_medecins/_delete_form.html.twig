<form method="post" action="{{ path('app_planning_medecins_delete', {'id': planning_medecin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planning_medecin.id) }}">
    <button class="btn">Delete</button>
</form>
