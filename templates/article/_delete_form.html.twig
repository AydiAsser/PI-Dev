{# {% extends 'base.html.twig' %} #}
{% block title%} {%endblock%}

{% block body%} 
          {# <a style="margin-left:20px;margin-bottom:20px;" class="genric-btn primary radius" href="{{ path('app_article_index') }}">back to list</a> #}

<form style="margin-left:20px;margin-bottom:20px;" method="post" action="{{ path('app_article_admin_delete', {'id': article.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.id) }}">
    <button class="genric-btn danger radius">Delete</button>
</form>
{%endblock%}
